import Foundation

/// Replace with your Supabase project URL and anon key (Settings → API in the Supabase dashboard).
enum AppSecrets {
  static let supabaseURL = URL(string: "https://YOUR_PROJECT_REF.supabase.co")!
  static let supabaseAnonKey = "YOUR_SUPABASE_ANON_KEY"
}
